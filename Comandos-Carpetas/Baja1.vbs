set WshShell = WScript.CreateObject("WScript.Shell")
Wshshell.SendKeys "{DOWN}"

'Este programa baja una vez por los archivos o carpetas.
'Solo tienes que copiar ese codigo en un archivo de bloc de notas, y después cambiar la extensión .txt por la extensión .vbs
'Es decir, si el archivo lo llamas "Baja1.txt" tu lo cambias a "Baja1.vbs"
'Luego vas a los comandos de jarvis y añades en la sección "programas" el comando que abrirá este programa y vinculas el comando al programa.
